# Operational SQL

> **Placeholders**: `{TARGET_DB}`, `{SCHEMA}`, `{WAREHOUSE}`, `{IATA}`, `{BACKFILL_DAYS}`

---

## Initial DT Refresh (run after all DTs created)

```sql
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.ADSB_DATA_LOCAL REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_AIRCRAFT_GROUND_SESSIONS REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_ADSB_GROUND_POINTS REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_FLIGHT_GATE_TIME REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_GATE_UTIL_DAILY REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_GATE_AIRLINE_DWELL_DAILY REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_FLIGHT_DWELL_WITH_AIRLINE REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_ADSB_DAILY REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_ADSB_HOURLY REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRACKER_FLIGHT_LIST REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_AIRLINE_TRAFFIC_DAILY REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_AIRLINE_DELAY_DAILY REFRESH;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.RUNWAY_CROSSINGS_DETAILED REFRESH;
```

---

## Resume Dynamic Tables

```sql
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_AIRCRAFT_GROUND_SESSIONS RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_ADSB_GROUND_POINTS RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_FLIGHT_GATE_TIME RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_GATE_UTIL_DAILY RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_GATE_AIRLINE_DWELL_DAILY RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_FLIGHT_DWELL_WITH_AIRLINE RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.ADSB_DATA_LOCAL RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_ADSB_DAILY RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_ADSB_HOURLY RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRACKER_FLIGHT_LIST RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_AIRLINE_TRAFFIC_DAILY RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_AIRLINE_DELAY_DAILY RESUME;
ALTER DYNAMIC TABLE {TARGET_DB}.{SCHEMA}.RUNWAY_CROSSINGS_DETAILED RESUME;
```

---

## Install-time Calls

Run these after all objects are created and tasks are resumed:

```sql
CALL {TARGET_DB}.{SCHEMA}.PROC_ENRICH_ADSB_WITH_SCHEDULE(2);
CALL {TARGET_DB}.{SCHEMA}.PROC_REFRESH_DERIVED();
CALL {TARGET_DB}.{SCHEMA}.PROC_SMOKE_CHECK('10');
CALL {TARGET_DB}.{SCHEMA}.PROC_START_BACKFILL_HISTORY();
CALL {TARGET_DB}.{SCHEMA}.PROC_START_BACKFILL_RETRY_UTC();
```

---

## Verification

```sql
SELECT 'GATE_ANALYSIS_ADSB_GROUND_POINTS' AS tbl, COUNT(*) AS cnt FROM {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_ADSB_GROUND_POINTS
UNION ALL SELECT 'GATE_ANALYSIS_AIRCRAFT_GROUND_SESSIONS', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_AIRCRAFT_GROUND_SESSIONS
UNION ALL SELECT 'GATE_ANALYSIS_FLIGHT_GATE_TIME', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_FLIGHT_GATE_TIME
UNION ALL SELECT 'GATE_ANALYSIS_GATE_UTIL_DAILY', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_GATE_UTIL_DAILY
UNION ALL SELECT 'GATE_ANALYSIS_GATE_AIRLINE_DWELL_DAILY', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_GATE_AIRLINE_DWELL_DAILY
UNION ALL SELECT 'GATE_ANALYSIS_FLIGHT_DWELL_WITH_AIRLINE', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.GATE_ANALYSIS_FLIGHT_DWELL_WITH_AIRLINE
UNION ALL SELECT 'ADSB_DATA_LOCAL', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.ADSB_DATA_LOCAL
UNION ALL SELECT 'FLIGHT_TRAFFIC_FACT_ADSB_DAILY', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_ADSB_DAILY
UNION ALL SELECT 'FLIGHT_TRAFFIC_FACT_ADSB_HOURLY', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_ADSB_HOURLY
UNION ALL SELECT 'FLIGHT_TRACKER_FLIGHT_LIST', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.FLIGHT_TRACKER_FLIGHT_LIST
UNION ALL SELECT 'FLIGHT_TRAFFIC_FACT_AIRLINE_TRAFFIC_DAILY', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_AIRLINE_TRAFFIC_DAILY
UNION ALL SELECT 'FLIGHT_TRAFFIC_FACT_AIRLINE_DELAY_DAILY', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.FLIGHT_TRAFFIC_FACT_AIRLINE_DELAY_DAILY
UNION ALL SELECT 'RUNWAY_CROSSINGS_DETAILED', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.RUNWAY_CROSSINGS_DETAILED
UNION ALL SELECT 'FLIGHT_SCHEDULE', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.FLIGHT_SCHEDULE
UNION ALL SELECT 'HELPER_FLIGHT_SCHEDULE_RAW', COUNT(*) FROM {TARGET_DB}.{SCHEMA}.HELPER_FLIGHT_SCHEDULE_RAW;
```
