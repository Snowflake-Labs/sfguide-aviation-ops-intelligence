# Single source of truth for aviation dashboard SPCS image tags.
# Bump this tag on each release; check_image_versions.sh validates
# all other files (service YAML, build-images.md, SKILL.md) match.
# The aviation-dashboard skill reads this at build and deploy time.
AVIATION_DASHBOARD_TAG=v1.0.0
